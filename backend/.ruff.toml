    # Указываем Ruff, какие файлы проверять
    select = ["E", "F", "W", "I"] # E=Error, F=Pyflakes (наша ошибка), W=Warning, I=isort
    
    # --- ВОТ ФИКС ---
    # "Игнорируй ошибку F401 (unused import) ТОЛЬКО в этих файлах"
    [per-file-ignores]
    "main.py" = ["F401"]
    "test_main.py" = ["F401"]
    "test_crud.py" = ["F401", "F811"] # F811 - Redefinition
    "test_models.py" = ["F401"]
    "models.py" = ["F401"] # Игнорируем 'Boolean'
    ```

### ## 2. (Опционально) Исправьте `test_models.py`

`Ruff` также пожаловался, что в `test_models.py` вы импортировали `pytest` и `sessionmaker`, но не использовали их. Он прав. **Удалите** эти две строки из `test_models.py`:

* `import pytest` (вверху)
* `from sqlalchemy.orm import sessionmaker` (вверху)

---
## ## 3. Что делать сейчас

1.  **Создайте** (или измените) `ruff.toml` в папке `backend/`.
2.  **Сделайте коммит** этого нового файла:
    ```bash
    git add .
    git commit -m "Add ruff.toml to ignore F401 errors"
    ```
3.  **Отправьте** его на GitHub:
    ```bash
    git push
    
